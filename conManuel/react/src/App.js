
import './App.css';
import Image from './components/Image';
import Counter from './components/Counter';
import Timer from './components/Timer';

function App() {
  return (
    <Timer />
  );
}

export default App;       // default -> quando noi utilizziamo questo file, ci aspettiamo che lo sviluppatore utilizzi quella fz (tutto ciò che è opzionale, può essere esportato, ma se ne ha bisogno si usano le {})
